/* Shared interaction-tuning constants for the input controllers. */

/** A pointer must travel at least this many client pixels before a press is
    treated as a drag rather than a click (compared as squared distance). */
export const DRAG_ARM_THRESHOLD_PX = 4;
