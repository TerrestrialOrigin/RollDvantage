import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.terrestrialorigin.rolldvantage',
  appName: 'rolldvantage',
  webDir: 'dist'
};

export default config;
